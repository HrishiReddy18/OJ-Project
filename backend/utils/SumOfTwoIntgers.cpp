#include <iostream>
using namespace std;
int main() {
    int num1, num2, sum;
    cin >> num1;
    cin >> num2;
    // Adding the two numbers
    sum = num1 + num2;
    // Displaying the result
    cout  << sum << endl;
    return 0;
}